accessible: true
author: Loan
categories: []
context: Copie ici le contenu de ton fichier `ia.py`
environment: default
evaluate: best
file: ''
groups: false
input_random: '0'
limits:
    time: '30'
    output: '2'
    memory: '100'
name: Concours Labyrinthe IA
network_grading: false
problems:
    code:
        header: |4-

            .. raw:: html

                <div class="alert alert-secondary" role="alert">
                    <h6>Ceci n'est pas un debugger, tester votre code localement avant de le soummettre</h6>
                </div>


            Primitives (sur Game) :
             - ``moveForward()`` : Avance le joueur devant lui (si la case n’est pas occupée par un mur)
             - ``turnLeft()`` / ``turnRight()`` : Effectue une rotation du joueur de 90° sur la gauche/droite
             - ``isFreeForward()`` / ``isGoalForward()`` -> ``bool`` : Vérifie si la case devant le joueur est libre/l’objectif
             - ``direction()`` -> ``str(right``/``up``/``left``/``down)`` : Donne la direction du joueur

            .. raw:: html

                <table class="table table-bordered table-sm">
                  <thead class="thead-light">
                    <tr>
                      <th></th>
                      <th scope="col"><code>moveForward</code></th>
                      <th scope="col"><code>turn...</code></th>
                      <th scope="col"><code>is...forward</code></th>
                      <th scope="col"><code>direction</code></th>
                    </tr>
                  </thead>
                  <tbody>
                    <tr>
                      <th scope="row">Cout (Point d'action)</th>
                      <td>5</td>
                      <td>2</td>
                      <td>1</td>
                      <td>0</td>
                    </tr>
                  </tbody>
                </table>

            N'oublie pas de définir la fonction ``step()`` qui contiendra les actions que doit effectuer le joueur à chaque étape!
        name: Code de l'IA
        language: python
        type: code
        default: |4

            def stepExample(game):
                if game.isFreeForward() or game.isGoalForward():
                    game.moveForward()
                else:
                    if game.direction() == "up":
                        game.turnRight()
                    else:
                        game.turnLeft()

            def step(game):
                stepExample(game)
run_cmd: ''
stored_submissions: 20
submission_limit:
    amount: -1
    period: -1
weight: 1.0
